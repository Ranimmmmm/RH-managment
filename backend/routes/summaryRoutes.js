const express = require('express');
const router = express.Router();
const { Activity, sequelize } = require('../db');  // Ensure your sequelize and Activity model are properly imported

// Endpoint to fetch all activities for a specific employee by year and month


module.exports = router;
